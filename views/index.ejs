
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DigiStall</title>
    <!-- Bootstrap CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #b3e5fc, #e1f5fe);
            color: #333;
            font-family: 'Poppins', sans-serif;
        }

        .spotify-card {
            background-color: WHITE;
            border-radius: 15px;
            width: 350px;
            padding: 2rem;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }

        .spotify-logo {
            width: 150px;
            margin-bottom: 1rem;
        }

        h4 {
            font-weight: 600;
            margin-bottom: 1rem;
        }

        p {
            margin-bottom: 1rem;
            line-height: 1.5;
        }

        a {
            color: #5bc0de;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }

        .btn-primary {
            background-color: #1a8caf;
            border-color: #1b6880;
        }

        .btn-primary:hover {
            background-color: #5bc0de;
            color: black;
        }

        .small {
            color: black;
        }

        .small:hover {
            color: #5bc0de;
        }

     
    </style>
</head>
<body>
    <div class="container d-flex justify-content-center align-items-center min-vh-100">
        <div class="card text-center spotify-card">
            <div class="mb-4">
                <img src="images/logodigistall.png" alt="DigiStall" class="spotify-logo">
            </div>
            <h4 class="mb-3">Looking for Gadgets?</h4>
            <p>Find them here at DigiStall!</p>
            <a href="/login" class="btn btn-primary btn-lg w-100 mb-3">Log in</a>
            <p class="small">New to DigiStall? <a href="/signup">Sign up</a></p>
            <p class="small">Authorized person only! <a href="/admin">Admin</a></p>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
